set(TEST_LAUNCH_DIR ${CMAKE_CURRENT_SOURCE_DIR}/test_launch_files)
set(PDDL_DIR ${CMAKE_CURRENT_SOURCE_DIR}/pddl)
set(CLIPS_DIR ${CMAKE_CURRENT_SOURCE_DIR}/clips)

install(DIRECTORY
  ${PDDL_DIR}
  ${CLIPS_DIR}
  DESTINATION share/${PROJECT_NAME}
)

add_subdirectory(unit)
